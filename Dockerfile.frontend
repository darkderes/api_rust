FROM nginx:alpine

# Copiar el frontend al directorio de nginx
COPY frontend/index.html /usr/share/nginx/html/

# Copiar configuración personalizada de nginx
COPY nginx.conf /etc/nginx/conf.d/default.conf

EXPOSE 80

CMD ["nginx", "-g", "daemon off;"]
